require "rails_helper"

RSpec.describe "User signs into an account" do
  context "User has an account" do
    it "happens successfully"
  end

  context "User does not have an account" do
    it "presents an error message"
  end
end
